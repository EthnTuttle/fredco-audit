# Runtime files — not committed
pipeline.db-shm
pipeline.db-wal
pipeline.lock
cron.lock
pipeline.pid

# Un-ignore pipeline.log (overrides root *.log rule) — committed for failure history
!pipeline.log

# Pre-download staging area (auto-deleted after transcription)
.staging/
